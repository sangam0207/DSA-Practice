#include<bits/stdc++.h>
using namespace std;

int main(){
    int a;
    cin>>a;
    if(a%7==0 && a%3==0) cout<<"the number is divisible by both 7 and 3\n";
    else cout<<" the number is not divisible by either 7 or 3\n";
    return 0;
}